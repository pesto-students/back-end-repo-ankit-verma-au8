name: TrackPe BE CI/CD

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Build Image
        run: |
          echo "Building image"
          ./build_docker.sh

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Test Image
        run: |
          echo "Testing image"
          ./docker_run_test_cases.sh

  push:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Login to AWS ECR
        run: |
          aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY_URI }}

      - name: Push Image
        run: |
          echo "Push image"
          docker tag regenera-core:latest ${{ secrets.ECR_REGISTRY_URI }}:$(date -u +"%Y%m%d%H%M%S")-${{ github.sha }}
          docker push ${{ secrets.ECR_REGISTRY_URI }}:$(date -u +"%Y%m%d%H%M%S")-${{ github.sha }}

  deploy_prod:
    runs-on: ubuntu-latest
    needs: push
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Deploy to Prod
        run: |
          echo "Deploying to Prod server"
          ~/deploy_server.sh
          echo "Deployment completed.."
